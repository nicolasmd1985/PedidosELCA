<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="seguridad.elca.pedidoselca">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Pedidos" />
        <activity
            android:name=".NewUser"
            android:label="@string/adduseractivitylabel"
            android:noHistory="true" />
        <activity android:name=".detalles_pedido" />
        <activity android:name=".Scaner_dispositivo" />
        <activity android:name=".Agregar_dispositivos"></activity>
    </application>

</manifest>